<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical"
        android:background="@color/surface">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Flow"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/textPrimary" />

        <ProgressBar
            android:id="@+id/progressSync"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnSync"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="Sync"
            android:textSize="12sp"
            style="@style/Widget.Material3.Button.OutlinedButton" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Summary cards -->
            <GridLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:columnCount="2"
                android:rowCount="2"
                android:layout_marginBottom="16dp">

                <include layout="@layout/card_summary"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:layout_margin="4dp"
                    android:id="@+id/cardSales" />

                <include layout="@layout/card_summary"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:layout_margin="4dp"
                    android:id="@+id/cardExpenses" />

                <include layout="@layout/card_summary"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:layout_margin="4dp"
                    android:id="@+id/cardPurchases" />

                <include layout="@layout/card_summary"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:layout_margin="4dp"
                    android:id="@+id/cardProfit" />
            </GridLayout>

            <!-- Summary values (overlaid via ViewModel directly) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="4dp">

                <TextView android:id="@+id/tvSalesLabel"  android:layout_width="0dp" android:layout_weight="1" android:text="Sales"     android:textColor="@color/textMuted" android:textSize="11sp" android:gravity="center"/>
                <TextView android:id="@+id/tvExpLabel"    android:layout_width="0dp" android:layout_weight="1" android:text="Expenses"  android:textColor="@color/textMuted" android:textSize="11sp" android:gravity="center"/>
                <TextView android:id="@+id/tvPurchLabel"  android:layout_width="0dp" android:layout_weight="1" android:text="Purchases" android:textColor="@color/textMuted" android:textSize="11sp" android:gravity="center"/>
                <TextView android:id="@+id/tvProfitLabel" android:layout_width="0dp" android:layout_weight="1" android:text="Profit"    android:textColor="@color/textMuted" android:textSize="11sp" android:gravity="center"/>
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="@color/surface"
                android:padding="12dp"
                android:layout_marginBottom="16dp">

                <TextView android:id="@+id/tvSales"     android:layout_width="0dp" android:layout_weight="1" android:text="₹0"  android:textColor="@color/green" android:textSize="16sp" android:textStyle="bold" android:gravity="center"/>
                <TextView android:id="@+id/tvExpenses"  android:layout_width="0dp" android:layout_weight="1" android:text="₹0"  android:textColor="@color/red"   android:textSize="16sp" android:textStyle="bold" android:gravity="center"/>
                <TextView android:id="@+id/tvPurchases" android:layout_width="0dp" android:layout_weight="1" android:text="₹0"  android:textColor="@color/blue"  android:textSize="16sp" android:textStyle="bold" android:gravity="center"/>
                <TextView android:id="@+id/tvProfit"    android:layout_width="0dp" android:layout_weight="1" android:text="₹0"  android:textColor="@color/green" android:textSize="16sp" android:textStyle="bold" android:gravity="center"/>
            </LinearLayout>

            <!-- Quick add buttons -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="16dp">

                <Button android:id="@+id/btnAddSale"
                    android:layout_width="0dp" android:layout_weight="1" android:layout_marginEnd="4dp"
                    android:text="+ Sale" android:backgroundTint="@color/green" />

                <Button android:id="@+id/btnAddExpense"
                    android:layout_width="0dp" android:layout_weight="1" android:layout_marginHorizontal="4dp"
                    android:text="+ Expense" android:backgroundTint="@color/red" />

                <Button android:id="@+id/btnAddPurchase"
                    android:layout_width="0dp" android:layout_weight="1" android:layout_marginStart="4dp"
                    android:text="+ Purchase" android:backgroundTint="@color/blue" />
            </LinearLayout>

            <!-- Transaction list header -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Recent Transactions"
                android:textColor="@color/textMuted"
                android:textSize="11sp"
                android:textAllCaps="true"
                android:letterSpacing="0.08"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tvEmpty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="No transactions yet. Add your first one above."
                android:textColor="@color/textMuted"
                android:textSize="14sp"
                android:gravity="center"
                android:padding="24dp"
                android:visibility="gone" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvTransactions"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
